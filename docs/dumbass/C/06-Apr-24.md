# C

> Programmers know what they are doing.

## CHAPTER 1

A function contains ***statement*** that specify the computing operations to be done.

Variables store values used during the computation.

In any context where it is permissiable to use the value of a variable of some type, you can use a more complicated expression of that type. 